<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Prueba.PageSettings"
             Title="Settings">
    <ContentPage.Content>
        <StackLayout>
            <Picker Title="Seleccione un color para la barra superior" x:Name="ColoresPicker"  ItemDisplayBinding="{Binding Nombre}">
                
            </Picker>
            <Picker Title="Seleccione un color de fondo" x:Name="FondoPicker" ItemDisplayBinding="{Binding Nombre}" Margin="0,25,0,0">
                
            </Picker>
            <StackLayout Orientation="Horizontal"  Margin="0,25,0,0">
                <Label Text="Modo Oscuro" VerticalTextAlignment="Center" x:Name="DarKModeLabel"></Label>
                <CheckBox x:Name="DarkModeCheckBox"/>
            </StackLayout>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>